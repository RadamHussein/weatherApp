 /*
 var weather = document.createElement("div");
 var response = JSON.parse(req.response);
 	weather.textContent = "City: " + response.name;
    weather.textContent = "Current Temp: " + response.main.temp;
    weather.textContent = "Wind: " + response.wind.speed;
    weather.textContent = "Precip: " + response.rain;
    document.body.appendChild(weather);
    */
